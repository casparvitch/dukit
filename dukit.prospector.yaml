
ignore-paths:
  - docs
  - examples
  - other
  - build
  - dist
  - dukit_conda_env

doc-warnings: false

pyroma:
  run: true

vulture:
  run: true

bandit:
  run: true

mccabe:
  run: true

mypy:
  run: true

# run (from dir with .git) via:
# `prospector --profile dukit.prospector.yaml -o grouped:prospector.log`